# 홈페이지 관리 체계 정비 설계

## 목표

글을 쓰고 공개하는 일은 간단하게 유지하면서, 공개 대상과 초안이 섞이지 않고 배포 전에 오류를 확인할 수 있게 한다.

## 현재 판단

- 칼럼의 정본이 원고, 목록 HTML, 개별 HTML로 나뉘어 있어 같은 정보를 여러 번 고쳐야 한다.
- `publish.py`가 다수의 페이지와 이미지를 매번 일괄 업로드해 작은 수정도 큰 발행 작업이 된다.
- 작업 폴더의 공개본, 초안, 실험 파일이 함께 있어 배포 범위를 판단하기 어렵다.
- 기존 Supabase 전환 결과는 유지하되, 정적 파일과 동적 뷰어가 혼재한 상태를 먼저 명확히 정리해야 한다.

## 운영 원칙

1. 공개 콘텐츠의 정본은 마크다운 한 곳에만 둔다.
2. HTML 목록과 개별 페이지는 정본에서 생성하거나, Supabase를 정본으로 채택한 뒤에는 동적 화면으로만 제공한다.
3. 초안과 실험 파일은 배포 대상과 분리한다.
4. 배포는 검증을 통과한 변경만 반영한다.

## 권장 구조

```text
프로젝트/
├── content/                 공개 원고의 정본
│   ├── columns.md
│   └── memos/
├── site/                    실제 공개 정적 파일
│   ├── index.html
│   ├── column.html
│   ├── columns/
│   ├── css/
│   ├── js/
│   └── img/
├── drafts/                  미공개 원고와 실험 페이지
├── archive/                 공개 종료 또는 보관 자료
├── scripts/                 생성과 검증 도구
└── docs/                    운영 문서와 작업 기록
```

처음부터 폴더를 옮기지 않는다. 1차 정비에서는 현재 구조를 유지하고, 공개 대상과 초안의 목록만 확정한다. 실제 이동은 배포 흐름이 안정된 뒤 한 번에 진행한다.

## 칼럼 발행 흐름

```text
원고 작성 → content의 원고 검토 → 목록·개별 페이지 생성 → 동기화 검사 → 변경 검토 → 배포
```

- 원고는 `💬 칼럼·기고.md`를 계속 정본으로 사용한다.
- `column.html`의 인덱스·카드·태그는 생성 대상으로 전환한다.
- `columns/*.html`도 같은 원고에서 생성한다.
- 이미지 경로와 칼럼 유형은 원고 헤더의 메타데이터 또는 별도 작은 메타데이터 파일 한 곳에서 관리한다.
- `check_columns_sync.py`는 한글 NFC/NFD를 정규화해 비교하고, 원고·목록·개별 페이지·이미지의 누락을 검사한다.

## 배포 흐름

### 1차 안정화

- macOS에서는 `🚀 사이트에 올리기.command`를 사용한다. Windows 배치 파일은 호환용으로만 유지한다.
- GitHub 토큰은 `🔐 GitHub 토큰 저장.command`로 macOS 키체인에 한 번만 저장하고, 발행 실행 파일이 실행 시에만 읽는다.
- 실행 전 `python scripts/check_columns_sync.py`와 링크·이미지 검사를 자동 실행한다.
- 실패하면 배포를 중단한다.
- `publish.py`는 공개 목록에 있는 파일만 반영하도록 관리한다.

### 2차 단순화

- 공개 변경을 Git 커밋으로 남긴 뒤 원격 저장소에 푸시한다.
- GitHub Pages가 저장소 변경으로 배포되도록 전환한다.
- API로 파일을 개별 업로드하는 `publish.py`는 비상용으로만 남기거나 제거한다.

## 즉시 정리 대상

1. 새 칼럼 두 편을 목록에 반영할지, 초안으로 둘지 결정한다.
2. 한글 파일명 정규화 때문에 발생하는 칼럼 동기화 검사 오탐을 수정한다.
3. 현재 수정·미추적 파일을 공개 예정, 초안, 보관으로 분류한다.
4. 공개할 변경만 별도 커밋으로 고정한다.

## 성공 기준

- 새 칼럼 한 편을 원고 한 곳에만 추가해도 목록과 상세 페이지가 생성된다.
- 배포 전 검사에서 목록·파일·이미지 누락이 발견되면 배포가 중단된다.
- 작업 폴더에서 공개 대상과 미공개 파일을 한눈에 구분할 수 있다.
- 작은 콘텐츠 수정이 전체 사이트를 수동으로 점검하는 일로 번지지 않는다.
