# 📓 Supabase 마이그레이션 작업 컨텍스트 노트

## 홈페이지 관리 체계 정비

## 하이테크 포트폴리오 랜딩 페이지

- 2026-08-10 현재 `index.html`을 포함해 다수 파일이 미배포 변경 상태다. 사용자의 새 랜딩 페이지 요청은 기존 변경을 덮어쓰지 않도록 `tech-portfolio.html` 독립 파일로 구현한다.
- 운영자 맥락과 실제 홈페이지의 신뢰성을 우선해 표기 이름은 `CHOI SEUNG-HWAN`, 연락처는 `7979@korea.kr`로 사용한다. 외부 사회관계망 URL은 운영자 계정 확인 전 서비스의 기본 URL로 두고 코드 주석으로 교체 지점을 표시한다.
- 시각적 서명은 공공행정·연구·도시를 연결하는 관측 신호망이다. Three.js를 사용하지 않고 Canvas 2D로 직접 구현해 외부 의존성과 로딩 부담을 줄인다.
- 모바일 브라우저 검사에서 원근 변환된 `.hero-grid`가 문서의 계산 너비를 늘리는 현상을 확인했다. 히어로에 `contain: paint`를 적용해 시각 효과를 해당 섹션 안에서 확실히 자른다.
- 브라우저 검사 결과 데스크톱에서 제목, 3개 작업 카드, 8개 기술 버튼, 타이핑 문구, Canvas 초기화가 확인됐다. 모바일 390px에서는 메뉴 버튼과 작업 카드 1열 레이아웃이 적용됐다.

## 리플릿 제작기

- 2026-07-31 기존 `리플릿 제작.html`은 Cocoa HTML Writer로 저장된 정적 시안이며 입력·업로드·인쇄 동작이 없었다.
- 홈페이지용 `leaflet.html`은 단일 정적 웹앱으로 구현한다. 사진과 로고는 브라우저 안에서만 Data URL로 처리하고, 저장된 버전은 해당 브라우저의 localStorage에 보관한다.
- 인쇄는 A4 가로 두 장으로 출력된다. 외면과 내면을 양면 인쇄해 리플릿으로 사용할 수 있으며, PDF 저장은 브라우저의 인쇄 대화상자를 사용한다.
- 브라우저 자동화로 3단·2단 전환, 문구·사진 추가, localStorage 버전 저장, 인쇄 매체의 A4 가로 시트 렌더링을 확인했다.
- 2026-07-31 `leaflet.html`과 `index.html`만 대상으로 GitHub API 업로드를 시도했으나, 키체인 토큰이 `Resource not accessible by personal access token` 403을 반환했다. 토큰의 대상 저장소 또는 Contents 쓰기 권한을 갱신한 뒤 재시도해야 한다.
- 2026-08-01 사용자가 제공한 `지자체-리플렛-제작기/index.html`은 기존 제작기보다 편집 기능이 완성돼 있어 `leaflet.html`의 정본으로 교체했다.

- 2026-07-28 진단에서 칼럼 원본은 28편, `column.html`의 목록과 카드는 27편, `columns/*.html`은 29개로 확인됐다.
- 실제 목록 누락 후보는 `20260718아무일도없었습니다`, `20260723정리하는프로젝트를정리하지못한한달` 두 편이다.
- 기존 동기화 검사는 macOS 파일명의 NFD와 HTML 슬러그의 NFC를 그대로 비교해 기존 파일까지 불일치로 표시한다. 비교 전에 Unicode NFC 정규화가 필요하다.
- `publish.py`는 다수의 페이지와 `img/` 파일을 전체 업로드하는 구조다. 1차로 배포 전 검증을 추가하고, 이후 GitHub Pages의 Git 기반 배포로 단순화한다.
- 기존 Supabase 전환 산출물은 보존한다. 정적 생성 방식과 Supabase 동적 방식 중 하나를 정본으로 확정하기 전에는 기능을 추가하지 않는다.
- 2026-07-28 1차 안정화에서 7월 18일 칼럼을 별도 원고에서 `💬 칼럼·기고.md`로 편입했고, 7월 18일·23일 칼럼을 `column.html` 목록과 카드에 공개 항목으로 반영했다.
- `🚀 사이트에 올리기.bat`은 `publish.py` 실행 전에 `scripts/check_columns_sync.py`를 실행하며, 검사 실패 시 배포를 중단하도록 보완했다.
- 동기화 검사는 칼럼 수·슬러그·카드 번호 외에 카드가 참조하는 로컬 `img/` 파일의 존재 여부도 검사한다.
- 운영 환경은 macOS로 확인됐다. `🚀 사이트에 올리기.command`가 동기화 검사 후 `publish.py`를 실행하도록 추가했고, `.bat` 파일은 호환용으로 유지한다.
- GitHub 토큰은 환경변수나 문서에 기록하지 않고, `🔐 GitHub 토큰 저장.command`로 macOS 키체인에 저장한다. 발행 실행 파일이 키체인에서 읽어 현재 실행에만 `GITHUB_TOKEN`으로 전달한다.

## 1. 개요 및 배경
- **목적**: 기존 GitHub Pages 기반 정적 개인 사이트의 콘텐츠(칼럼 및 정리노트 4종)를 Supabase 데이터베이스로 이전하여 dynamic 콘텐츠 관리를 가능하게 하고, 로컬 빌드 및 Git 푸시 의무에서 해방됨.
- **아키텍처 모델**: 하이브리드 아키텍처 (Hybrid Architecture)
  - **호스팅**: 무료이고 전 세계 CDN이 보장되는 **GitHub Pages** 계속 사용.
  - **백엔드**: **Supabase PostgreSQL & REST API** 사용.
  - **콘텐츠 관리**: Supabase 대시보드나 간이 업로드 스크립트를 통해 언제 어디서나 신규 글 업로드 및 실시간 배포 가능.

## 2. 주요 설계 의사결정

### 2.1 데이터베이스 테이블 설계
1.  **`columns` (칼럼 및 기고)**
    - 기존 `build_columns.py`에서 파싱하고 `column.html` 인덱스 리스트에 매핑되어 있는 슬러그, 제목, 날짜, 태그, 본문 내용, 이미지 정보를 단일 관계형 테이블로 구조화.
    - 슬러그(`slug`)를 고유식별자(Unique)로 설정하여, 개별 칼럼 상세조회 시 URL 파라미터(`column-view.html?slug=슬러그`)로 자연스럽게 라우팅 가능하도록 함.
2.  **`memos` (4대 정리노트)**
    - `💡 아이디어·기획`, `🎯 목표·계획·다짐`, `📖 일상·감정·회고`, `📚 업무·논문·공부` 4개의 마크다운 파일을 개별 테이블로 쪼개지 않고, 단일 `memos` 테이블의 `category` 컬럼(`ideas`, `goals`, `diary`, `work`)으로 완벽히 통합하여 유지보수성 극대화.

### 2.2 보안 및 권한 제어 (RLS)
- Supabase의 가장 강력한 장점인 Row Level Security (RLS)를 활용.
- **조회(SELECT)**: 일반 익명 사용자(`anon`)를 위해 완전 공개 허용 (`true`).
- **쓰기/수정/삭제 (INSERT, UPDATE, DELETE)**: 기본 익명 키로는 불가능하도록 RLS 차단. 마이그레이션 파스크립트 및 향후 어드민 관리에서는 비밀 환경변수로 관리되는 `service_role` 키를 사용하여 안전하게 보호.

### 2.3 프론트엔드 최적화
- 기존에 각 칼럼마다 생성되던 수십 개의 정적 HTML 파일들을 하나의 동적 뷰어 `column-view.html`로 대체.
- 마크다운 파싱을 클라이언트 사이드에서 처리하기 위해, 경량 마크다운 렌더러 라이브러리(marked.js)를 CDN으로 연동하여 풍부한 서식과 스타일 보존.
