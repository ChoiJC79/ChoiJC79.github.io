# 📋 Supabase 마이그레이션 작업 체크리스트

## 홈페이지 관리 체계 정비

## 하이테크 포트폴리오 랜딩 페이지

- [x] 기존 홈페이지의 미배포 변경을 보존하기 위해 독립 HTML 파일로 구현한다.
- [x] 6개 섹션, Canvas 입자 배경, 반응형 인터랙션을 단일 파일로 구성한다.
- [x] 브라우저에서 데스크톱·모바일 화면과 상호작용을 검증한다.
- [ ] 운영자 확인 뒤 `index.html` 교체 여부를 결정한다.

## 리플릿 제작기

- [x] 리플릿 편집기와 A4 인쇄 레이아웃을 구현한다.
- [x] 사진·텍스트·로고·버전 저장·PDF 인쇄 기능을 구현한다.
- [x] 로컬 브라우저 자동화로 편집과 인쇄 흐름을 검증한다.
- [x] 홈페이지 링크와 발행 대상에 등록한다.

- [x] 새 칼럼 2편의 공개 여부를 결정하고 목록과 상세 페이지 상태를 일치시킨다.
- [x] `check_columns_sync.py`의 한글 파일명 NFC/NFD 정규화 오탐을 수정한다.
- [ ] 현재 변경 파일을 공개 예정, 초안, 보관으로 분류한다.
- [x] 공개 대상 파일 목록과 배포 전 검증 항목을 확정한다.
- [ ] 칼럼 목록·카드·상세 페이지를 단일 원고에서 생성하도록 전환한다.
- [x] 검증 성공 시에만 배포하도록 배포 실행 파일을 보완한다.
- [x] macOS용 발행 실행 파일을 추가한다.
- [ ] Git 커밋·푸시 기반 GitHub Pages 배포 전환 여부를 결정한다.

- [x] **1단계: Supabase 데이터베이스 테이블 및 RLS 정책 설계**
  - [x] `columns` 테이블 SQL 스키마 정의 (`id`, `slug`, `title`, `date`, `tags`, `body`, `type`, `image_url`, `created_at`)
  - [x] `memos` 테이블 SQL 스키마 정의 (`id`, `category`, `date`, `content`, `created_at`)
  - [x] 두 테이블에 대한 Row Level Security (RLS) 활성화 및 전체 공개 SELECT 조회 권한 설정 (SELECT Policy)
  - [x] 데이터 수정을 위한 서비스 역할(Service Role) 기반 또는 관리용 세팅 확인

- [x] **2단계: 파이썬 마이그레이션 스크립트 작성 (`migrate_to_supabase.py`)**
  - [x] 마크다운 파서 및 데이터 정규화 로직 구현 (`💬 칼럼·기고.md` 및 `column.html` 메타데이터 연동)
  - [x] 4대 정리노트 마크다운 파서 구현 (`💡 아이디어·기획.md`, `🎯 목표·계획·다짐.md`, `📖 일상·감정·회고.md`, `📚 업무·논문·공부.md`)
  - [x] Supabase REST API 연동을 통한 일괄 동기화(Upsert/Insert) 로직 개발
  - [x] 로컬 실행 및 업로드 동작 테스트 및 검증

- [x] **3단계: 동적 프론트엔드 연동 개발**
  - [x] 단일 칼럼을 동적으로 보여주는 상세 뷰어 `column-view.html` 신규 생성 (마크다운 파서 JS 탑재)
  - [x] 칼럼 목록 페이지 `column.html`을 Supabase에서 실시간으로 목록을 가져오도록 수정
  - [x] 정리노트 페이지 `memo.html`을 Supabase에서 카테고리별 데이터를 실시간 로드해 표시하도록 수정

- [x] **4단계: 통합 테스트 및 검증**
  - [x] 로컬 개발 환경에서 화면 로딩 속도, 검색, 태그 필터링 검증
  - [x] 라이트/다크 테마 통합 및 CSS 레이아웃 정렬 확인
  - [x] 최종 빌드 및 정상 작동 여부 검증
